name: EOD Trading Session

on:
  schedule:
    # 4:15 PM EDT (UTC-4) = 20:15 UTC  — valid Apr–Nov
    # 4:15 PM EST (UTC-5) = 21:15 UTC  — update cron to '15 21' Nov–Mar
    - cron: '15 20 * * 1-5'
  workflow_dispatch:   # manual trigger for testing

jobs:
  eod:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # needed to commit portfolio.json back to the repo

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run EOD session
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python eod_session.py

      - name: Commit portfolio state
        run: |
          git config user.name "Trading Bot"
          git config user.email "trading-bot@noreply.github.com"
          git add docs/portfolio.json
          git diff --staged --quiet || git commit -m "session: EOD update $(date -u '+%Y-%m-%d')"
          git push
